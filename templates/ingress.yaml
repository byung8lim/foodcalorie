apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
  name: foodcalorie
  namespace: fitness
spec:
  ingressClassName: nginx
  rules:
  - host: foodcalorie.wbl.com
    http:
      paths:
      - backend:
          service:
            name: foodcalorie
            port:
              number: 8080
        path: /
        pathType: ImplementationSpecific
status: {}